const express = require("express");
const router = express.Router();

const {} = require("../../controllers/menu_controller");
const { upload } = require("../../middlewares/multer");

// CREATE

// UPDATE

// DELETE

module.exports = router;
